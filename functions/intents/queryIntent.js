function processQueryIntent(query_item) {
    switch (query_item) {
        case "temperature":
            break;
        case "humidity":
            break;
        default:
            break;
    }
}